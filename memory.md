
- [2026-09-19T01:56:24.863Z] mempalace-mcp 移植專案在 /home/sensei/works/mempalace-mcp。核心設計：TypeScript wrapper 作為 MemPalace Python MCP server 的 long-lived MCP client，spawn `python -m mempalace.mcp_server --transport stdio --palace <path>` 後重用於全部工具呼叫；45 個工具由 src/core/tools-catalog.ts 單一真源提供給 toolsProvider.ts(LM Studio Plugin) 與 mcp.ts(獨立 MCP server, 用 McpServer.registerTool) 兩入口。MemPalace/ 原始碼永不修改。
- [2026-09-19T02:41:49.002Z] mempalace-mcp 是 CommonJS 專案（package.json 不設 type 欄位），因為 lms dev / Hub 用 esbuild 把 plugin 打包成 CJS，必須以 CJS 載入。後果：(1) 原始碼用 __dirname 而非 import.meta 定位 .venv；(2) src/index.ts 必須 export 一個 main(pluginContext) 函式（.lmstudio/entry.ts 會呼叫 module.main(pluginContext)），在其中用 pluginContext.withToolsProvider(toolsProvider).withGlobalConfigSchematics(globalConfigSchematics)。@lmstudio/sdk v1.5.0 提供 cjs+esm 兩版。