{
  "name": "mempalace-mcp",
  "version": "0.1.0",
  "description": "MemPalace MCP server wrapper for LM Studio — a lightweight TypeScript wrapper that re-exposes all MemPalace MCP tools as an LM Studio Plugin tool provider and as a standalone MCP server, without modifying the original Python source tree.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "lms dev",
    "push": "lms push",
    "typecheck": "tsc --noEmit",
    "mcp": "node dist/mcp.js",
    "setup": "node scripts/setup.cjs",
    "postinstall": "node scripts/setup.cjs"
  },
  "author": "hoonowng",
  "license": "MIT",
  "bin": {
    "mempalace-mcp-lms": "dist/mcp.js"
  },
  "dependencies": {
    "@lmstudio/sdk": "^1.5.0",
    "@modelcontextprotocol/sdk": "^1.20.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^22.13.1",
    "typescript": "^5.7.2"
  }
}
